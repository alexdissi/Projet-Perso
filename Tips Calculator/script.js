const inputs = document.querySelectorAll('.calculatorInput');
const buttons = document.querySelectorAll('.selectTip');
const tipValue = document.getElementById('tip');
const billValue = document.getElementById('bill');
const peopleValue = document.getElementById('nbrPeople');
const tipAmount = document.getElementById('tip-amount');
const totalPer = document.getElementById('total-per');

